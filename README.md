# magic-alt.github.io

使用 Jekyll 与 GitHub Pages 搭建的个人博客。

## 本地预览

1. 安装 Ruby 与 Bundler。
2. 安装依赖：
   ```bash
   bundle install
   ```
3. 本地运行：
   ```bash
   bundle exec jekyll serve
   ```

## 部署到 GitHub Pages

- 推送仓库到 GitHub。
- 在仓库 Settings → Pages 中，将发布源设置为默认分支的根目录。
